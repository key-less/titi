# Feature: Dashboard

Dashboard operacional HELPDEX: métricas (tiempo de respuesta, resueltos, abiertos, SLA, horas), gráficos (respuesta vs SLA, semana), resumen mensual, estado de parches Datto RMM y tabla de tickets recientes con panel expandible y botones "Ver en AutoTask" / "Preguntar a IA".

Los datos son mock; al conectar el backend se usarán hooks/API en `api/` o `hooks/`.
