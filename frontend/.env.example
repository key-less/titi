# URL del backend para el proxy de Vite en desarrollo.
# Por defecto (vacío o sin definir) = http://127.0.0.1:8000 (php artisan serve).
# Con Laravel Herd, pon la URL del sitio enlazado, por ejemplo:
# VITE_BACKEND_URL=http://helpdex.test
